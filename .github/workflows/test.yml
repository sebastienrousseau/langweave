# DEPRECATED: This workflow has been replaced by ci.yml
# The new comprehensive CI pipeline includes all testing functionality
# and more stringent requirements. This file is kept for reference only.

name: 🧪 Test (DEPRECATED)

on:
  # Disabled - replaced by ci.yml
  # push:
  # pull_request:

jobs:
  deprecated_notice:
    name: Deprecated Notice
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️ This workflow has been deprecated and replaced by ci.yml"
          echo "The new CI pipeline provides comprehensive testing with:"
          echo "- Zero-warning policy enforcement"
          echo "- 100% code coverage requirement"
          echo "- Cross-platform testing (macOS + Linux)"
          echo "- Security scanning"
          echo "- Performance regression detection"
          echo ""
          echo "Please refer to .github/workflows/ci.yml for the active pipeline."
