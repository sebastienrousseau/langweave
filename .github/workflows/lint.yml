# DEPRECATED: This workflow has been replaced by ci.yml
# The new comprehensive CI pipeline includes enhanced linting functionality
# with zero-warning policy enforcement across all platforms.

name: 🧪 Lint (DEPRECATED)

on:
  # Disabled - replaced by ci.yml comprehensive pipeline
  # push:
  # pull_request:

jobs:
  deprecated_notice:
    name: Deprecated Notice
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️ This workflow has been deprecated and replaced by ci.yml"
          echo "The new CI pipeline provides enhanced linting with:"
          echo "- Global warnings-as-errors enforcement via RUSTFLAGS"
          echo "- Cross-platform linting (macOS + Linux)"
          echo "- Integration with formatter and type checks"
          echo "- Comprehensive clippy configuration from Cargo.toml"
          echo ""
          echo "Please refer to .github/workflows/ci.yml for the active pipeline."
